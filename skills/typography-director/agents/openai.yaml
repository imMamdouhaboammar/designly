interface:
  display_name: "Typography Director"
  short_description: "Type hierarchy, measure & exact-copy preservation"
  icon_small: "./assets/small-logo.svg"
  icon_large: "./assets/large-logo.svg"
  brand_color: "#111111"
  default_prompt:
    - "Set typographic hierarchy, scale, measure, and semantic line breaks for this headline."
    - "Lock exact user copy and define dedicated text zones with legibility contrast."
policy:
  allow_implicit_invocation: true
